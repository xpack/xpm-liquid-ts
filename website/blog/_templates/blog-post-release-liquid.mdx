---

title:  v{{releaseVersion}} released
seo_title: Version {{releaseVersion}} released
description: Version v{{releaseVersion}} is a TODO new/maintenance release
keywords:
  - xpack
  - xpm-lib
  - release

date: {{releaseDate}}

authors: ilg-ul

# To be listed in the Releases page.
tags:
  - releases

# ----- Custom properties -----------------------------------------------------

version: "{{releaseVersion}}"

release_tag_url: "https://github.com/xpack/xpm-lib-ts/tree/v{{releaseVersion}}"

---

import {PageMetadata} from '@docusaurus/theme-common';
import Image from '@theme/IdealImage';
import CodeBlock from '@theme/CodeBlock';
import Link from '@docusaurus/Link';

import CommonPrerequisites from './_common/_prerequisites.mdx';
import CommonDownloadAnalytics from './_common/_download-analytics.mdx';

TODO: select one summary

Version **{{releaseVersion}}** is a maintenance release; it resolves ...

Version **{{releaseVersion}}** is a new release; it adds ....

<!-- truncate -->

<PageMetadata title={frontMatter.seo_title} />

The [](https://xpack.github.io/xpm-lib-ts/)
project is a TypeScript and dual mode CommonJS/ECMAScript Node.js module.

<CommonPrerequisites nodeVersion="20.0"/>

## Installation

For installation instructions, please refer to the
[Installation Guide](/docs/install/)
page.

## Import

To include this module within TypeScript and ECMAScript modules, utilise `import`:

```typescript
import { Xxx } from '@xpack/xpm-lib'
```

In JavaScript with CommonJS, utilise `require()`:

```javascript
const { Xxx } = require('@xpack/xpm-lib')
```

## Source code

- <Link to={frontMatter.release_tag_url}>GitHub tag v{frontMatter.version} tree</Link>

## Bug fixes

- none

## Enhancements

- none

## Known problems

- none

## CI tests

Before publishing, a set of tests were performed.
The results are available from:

- [GitHub Actions](https://github.com/xpack/xpm-lib-ts/actions)

